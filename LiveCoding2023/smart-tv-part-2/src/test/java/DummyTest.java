import static junit.framework.TestCase.assertEquals;

import org.junit.Test;

/**
 * Just a dummy test to make sure all test dependencies and imports work.
 * Delete this when the real tests are in place!
 */
public class DummyTest {
  @Test
  public void testNonsense() {
    assertEquals(4, 2 + 2);
  }
}
